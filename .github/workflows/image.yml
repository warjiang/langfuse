name: Build and push image

on:
  push:
    branches: ["feat/deploy-with-private-registry"]
env:
  REGISTRY: "ghcr.io"
  LANGFUSE_WEB_IMAGE_NAME: ${{ github.repository }}
  LANGFUSE_WORKER_IMAGE_NAME: ${{ github.repository }}-worker
  NEXT_PUBLIC_BASE_PATH: ${{ secrets.NEXT_PUBLIC_BASE_PATH }}
  NEXTAUTH_URL: ${{ secrets.NEXTAUTH_URL }}
jobs:
  build-lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Log into registry ${{ env.REGISTRY }}
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Start containers
        run: docker compose -f "docker-compose.build.yml" build

      - name: Check images
        run: docker images
      - name: Push images
        run: |
          docker push ${{ env.REGISTRY }}/${{ env.LANGFUSE_WEB_IMAGE_NAME }}
          docker push ${{ env.REGISTRY }}/${{ env.LANGFUSE_WORKER_IMAGE_NAME }}
